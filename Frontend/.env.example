# Supabase (public/publishable values — safe to expose to the browser)
NEXT_PUBLIC_SUPABASE_URL="https://lezpnfelybhztkcivtam.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="<your-supabase-anon-or-publishable-key>"

# NestJS API base (server-side only; used by the BFF proxy)
BACKEND_URL="http://localhost:3001"

# White-label product name shown on the login screen and as the in-app subtitle.
# Each client's own organization name is shown inside the app (from their org record).
NEXT_PUBLIC_APP_NAME="CRM Platform"
